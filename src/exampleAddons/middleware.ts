// import type { Middleware, MiddlewareAPI, Dispatch, AnyAction } from '@reduxjs/toolkit'

// export const print1: Middleware = (storeAPI: MiddlewareAPI) => (next: Dispatch<AnyAction>) => (action: AnyAction) => {
//   console.log('1')
//   return next(action)
// }

// export const print2: Middleware = (storeAPI: MiddlewareAPI) => (next: Dispatch<AnyAction>) => (action: AnyAction) => {
//   console.log('2')
//   return next(action)
// }

// export const print3: Middleware = (storeAPI: MiddlewareAPI) => (next: Dispatch<AnyAction>) => (action: AnyAction) => {
//   console.log('3')
//   return next(action)
// }