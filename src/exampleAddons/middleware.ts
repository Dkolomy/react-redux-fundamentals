// import type { Middleware, MiddlewareAPI, Dispatch } from '@reduxjs/toolkit'

// export const print1: Middleware = (storeAPI: MiddlewareAPI) => (next: Dispatch<unknown>) => (action: unknown) => {
//   console.log('1')
//   return next(action)
// }

// export const print2: Middleware = (storeAPI: MiddlewareAPI) => (next: Dispatch<unknown>) => (action: unknown) => {
//   console.log('2')
//   return next(action)
// }

// export const print3: Middleware = (storeAPI: MiddlewareAPI) => (next: Dispatch<unknown>) => (action: unknown) => {
//   console.log('3')
//   return next(action)
// }